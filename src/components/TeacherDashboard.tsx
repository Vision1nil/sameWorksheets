'use client'

// This is a temporary placeholder file to satisfy build dependencies
// It will be removed once the application is fully migrated to student-only

import { getStudentProgress } from '@/lib/database'

// Dummy export to satisfy build process
export const getTeacherClassrooms = async () => {
  return []
}

export default function TeacherDashboard() {
  return null
}
